version: "3"
services:
  server:
    image: juhyunglee/do-it-client:latest
    restart: on-failure
    container_name: do-it-client-red
    ports:
      - 3000:3000

networks:
  default:
    name: doitNetwork